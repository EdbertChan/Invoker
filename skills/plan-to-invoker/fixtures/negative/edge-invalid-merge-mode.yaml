# Edge case: Invalid approvalMode enum value
# Expected error: invalid_enum_value (field: approvalMode)
#
# approvalMode must be one of: external_review, manual, automatic

name: "Invalid approvalMode enum"
repoUrl: git@github.com:example-org/acme-repo.git
approvalMode: invalid_mode

tasks:
  - id: test-task
    description: "A test task"
    command: "echo test"
    dependencies: []
